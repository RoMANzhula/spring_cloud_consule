spring:
  profiles:
    active: default
  config:
    import: 'consul:'
  application:
    name: order-service
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        instance-id: order-service-i1
        acl-token: 7557ff23-a0ce-91da-8c50-011b30ff70c3
      config:
        acl-token: 7557ff23-a0ce-91da-8c50-011b30ff70c3
  sql:
    init:
      mode: never # change to 'always' for first running ('never' - for all others)

server:
  port: 0
